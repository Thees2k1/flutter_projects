name: poke_api_integration
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.9.2
  provider: ^6.1.5+1
  json_annotation: ^4.11.0
  drift: ^2.32.0
  drift_flutter: ^0.3.0
  google_fonts: ^8.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  json_serializable: ^6.13.0
  build_runner: ^2.12.2
  drift_dev: ^2.32.0

flutter:
  uses-material-design: true
